server:
  port: 9000
  
spring:
  application:
    name: gateway
  #cloud:
  #  gateway:
  #    routes:
        #- id:
        #  uri: lb://post/post
        #  predicates:
        #  - Path=/post/**

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
